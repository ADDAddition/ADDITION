name: addition_app
description: "Addition Core — ADDITION full-node desktop GUI (loopback write RPC)."
publish_to: 'none'

version: 1.1.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.5.0
  video_player: ^2.10.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/banners/addition-banner-1-poster.jpg
    - assets/banners/addition-banner-2-poster.jpg
